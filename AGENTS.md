# AGENTS.md

## GitHub = Single Source of Truth

이 repo의 정본은 GitHub `markshin1988` 계정이다. 모든 AI(Claude, Codex/GPT 등)와 모든 기기는 이 규칙을 따른다.

1. **작업 시작 전** 반드시 `git pull` — 다른 환경에서 코드가 수시로 올라온다.
2. **수정 완료 즉시** `git add -A && git commit -m "<설명>" && git push` — 미루지 않는다.
3. 채팅/로컬에 남은 옛 사본을 기준으로 삼지 말 것. 항상 GitHub 최신이 기준.
